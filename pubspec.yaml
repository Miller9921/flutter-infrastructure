name: infrastructure
description: Capa de infraestructura - Implementaciones y data sources
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  json_annotation: ^4.8.1
  http: ^1.1.0
  dartz: ^0.10.1

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  lints: ^3.0.0
