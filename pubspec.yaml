name: infrastructure
description: Capa de infraestructura - Implementaciones de repositorios y data sources
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Pending: Add domain package as dependency
  # domain:
  #   path: ../domain
  
  json_annotation: ^4.8.1
  dartz: ^0.10.1

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  test: ^1.24.0
  lints: ^3.0.0

# This package does not depend on Flutter
# It's a pure Dart package
